{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build service",
            "type": "shell",
            "options": {
                "cwd": "${workspaceFolder}/cmd/api-server"
            },
            "command": "go",
            "args": [
                "build",
                "-tags",
                "dev",
                "-gcflags",
                "all=-N -l",
                "-ldflags", "-X main.version=VSCODE_DEV_BUILD",
                "-o",
                "build/server"
            ]
        },
    ]
}